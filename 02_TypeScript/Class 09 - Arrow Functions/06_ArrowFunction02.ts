// Arrow Function with One Parameter

let printNum = (num: number) => { console.log(num) };

printNum(2);

// Arrow Function with Two or More Parameters
let sum = (n1: any, n2: any) => console.log(n1 + n2);

sum(3, 4);